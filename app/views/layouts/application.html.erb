<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Puppy-Sit" %></title>

    <%= stylesheet_link_tag    "application" %>
    <link rel="stylesheet" href="foundation-icons/foundation-icons.css" />
    <link href='http://fonts.googleapis.com/css?family=Raleway:500' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Lora' rel='stylesheet' type='text/css'>
    <%= javascript_include_tag "vendor/modernizr" %>

    <%= csrf_meta_tags %>
  </head>
  <body>
  <nav class = "top-bar">
  <section class = "title-area">
  <ul class = "left">
    <li class = "name"><%= link_to "Puppy Sitter", requests_path%></li>
  </ul>
  </section>
  <section class = "top-bar-section">
  <ul class = "right">
    <% if current_user%>
      <li>
      <a href="#">My Profile</a>
      <ul class = "profile-dropdown">
        <li><%= link_to "Home", profile_user_path(current_user)%></li>
        <li><%= link_to "View Your Requests",profile_user_requests_path(current_user.id)%> </li>
        <li><%= link_to "View Your Responses", profile_user_responses_path(current_user.id)%></li>
        <li><%= link_to "View Pending Reviews",profile_user_reviews_path(current_user.id)%> </li>
      </ul>
      </li>
      <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
    <%else%>
        <li><%= link_to "Sign in", new_user_session_path %></li>
        <li><%= link_to "Sign up", new_user_registration_path%></li>
    <%end%>
   </ul>
   </nav>
   <p class="notice" id = "message"><%= notice %></p>
   <p class="alert" id = "message"><%= alert %></p>

  <%= yield %>
  <div id="footer">
      This page is only a demo, not a real puppy sitting site. Other users will get access to your email address if you request or respond to a puppy sitting.
  </div>
  <script src='https://api.tiles.mapbox.com/mapbox.js/v2.1.2/mapbox.js'></script>
  <link href='https://api.tiles.mapbox.com/mapbox.js/v2.1.2/mapbox.css' rel='stylesheet' />
  <%= javascript_include_tag "application" %>
  </body>
</html>
